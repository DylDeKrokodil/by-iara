meta {
  name: Admin Rules List
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/admin/availability/rules
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("returns rules list", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
