meta {
  name: Admin Blocks List
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/admin/availability/blocks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("returns blocks list", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
