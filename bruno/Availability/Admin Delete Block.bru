meta {
  name: Admin Delete Block
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/admin/availability/blocks/{{blockId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("deletes a block", function () {
    expect(res.status).to.equal(204);
  });
}
