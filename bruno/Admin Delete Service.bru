meta {
  name: Admin Delete Service
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/admin/services/{{serviceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("soft-deletes the service", function () {
    expect(res.status).to.equal(204);
  });
}
