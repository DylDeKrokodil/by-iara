meta {
  name: Admin Services List
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/admin/services
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("lists all services for admins", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
